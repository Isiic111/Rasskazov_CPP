#ifndef ENUMS_H
#define ENUMS_H

enum class AccountType {
    CURRENT,
    SAVINGS
};

enum class ClientType {
    REGULAR,
    PREMIUM
};

enum class TransactionType {
    DEPOSIT,
    WITHDRAWAL,
    TRANSFER
};

#endif